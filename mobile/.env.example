# Скопируйте в .env при необходимости (локально не коммитить секреты).
# База API совпадает с backend Nest (см. backend README).

# Локально / эмулятор к хосту
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:4000

# Продакшен API (тот же хост, что NUXT_PUBLIC_API_BASE у фронта):
# EXPO_PUBLIC_API_BASE_URL=https://api.tournament-platform.ru
